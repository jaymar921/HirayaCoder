# /app

Extension source code goes here. Populate per the folder layout and build order in
[`/setup/PROMPT.md`](../setup/PROMPT.md) sections 3 and 9.

Expected subfolders once built out: `core/`, `agent/`, `features/`, `security/`, `webview/`, `utils/`, plus `extension.js` as the activation entrypoint.
